# Luna 操作 ChatGPT Pro

`luna-chatgpt-pro-operator` 让 GPT-5.6 Luna Max 根据主 Sol 已确认的任务包，通过 Codex 内置浏览器操作 ChatGPT Pro，上传已审核附件、恢复对话并结构化回传候选交付与证据。Luna 不修改工作树、不运行补丁、不提交或发布；主 Sol 负责脱敏打包、本地集成、测试和最终验收。

```text
让 luna_worker 使用 $luna-chatgpt-pro-operator，把已审核任务包交给 ChatGPT Pro，并回传 handoff envelope。
```

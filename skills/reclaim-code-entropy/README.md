# 代码熵回收

用于功能持续迭代后的仓库级减法：识别死代码、重复事实、投机抽象、额外路由、生命周期重复和迁移残留，并用消费者、兼容入口、Git 历史与验证证据决定是否处理。

调用示例：

```text
使用 $reclaim-code-entropy 审计最近 6 个已合并 PR 带来的代码熵
使用 $reclaim-code-entropy 直接收敛当前模块中证据最强的一个冗余边界
```

审计请求默认只读；只有明确要求实施时才修改。首版不提供扫描脚本，不以减少行数为目标，也不自动改动公开 API、持久化格式或安全边界。
